# DialedIn recipe picker polish

This patch:
- reduces the recommendation card to grind value + confidence only,
- fixes the double focus/input shell on Maximum next change on iOS,
- replaces the native recipe-method select with the same SVG icon picker used on Overview.

Extract into the repository root and replace existing files.
